# AGENTS.md — 竹南冷凍倉儲公司 庫存管理系統

> 本檔案是給 AI 代理（Claude Code / Codex / Cursor 等）閱讀的專案說明。
> 任何代理開始工作前，先讀完本檔，再讀 `docs/` 底下對應階段的文件。

## 1. 專案背景（Why）

課程：1151 系統分析與設計，6 人小組，組員各自用 Claude Code 協作。個案來源：`docs/個案/竹南冷凍公司個案.pdf`、`docs/個案/竹南-新版.pdf`（劉士豪，1995）。老師沒有技術要求，報告時能展示出系統即可。

- **客戶**：李太太，苗栗竹南「竹南冷凍倉儲公司」負責人。
- **業務**：兩座冷凍庫，盛產期低價買入蔬果（約 20 種），非盛產期高價賣出。採購量與時機全憑李太太經驗。
- **人力**：搬運工原本 3 人，現剩 1 人 + 李太太兩個兒子兼任。
- **痛點**：貨到就隨便找空位堆，出貨時在冷凍庫裡「憑運氣」找。曾經 5 籠甘藍菜找不到，幾個月後出現時已壓爛。
- **李太太的要求**：庫存管理電腦化，「這個系統一定要成功，否則她寧可收攤」。

### 老師（個案作者）的核心觀點 —— 系統設計必須遵守

1. **向上看三層**：李太太說「我要用電腦」，她真正要的是「解決問題、讓公司繼續賺錢」。我們是醫師不是藥劑師，不要照單抓藥。
2. **資訊管理的專業是「呈現事實」**：找出決策者，在正確的時間、地點，用正確的格式把正確的事實呈現給他。
3. **決策者與決策**：
   - **搬運工**（含兩個兒子）：進冷凍庫前就要能「預測」貨在哪個櫃位 → 需要「品項 → 櫃位」的即時對照。
   - **李太太**：決定採購數量與時機、決定出哪一批貨 → 需要各品項庫存數量、入庫日期（先進先出）、歷史進出紀錄。
4. **真實世界的模型**：系統就是倉庫的「數位模型」。第一步是把每個櫃位標準化、編號。模型要「篩除與決策無關的雜訊」，並且可以呈現過去、預測未來。
5. **員工首要職責是讓模型與真實世界同步**（7-11 店員每次銷售都要掃條碼）：入庫、出庫、移位每一筆都必須即時記錄，系統設計要讓「記錄」比「不記錄」更容易。
6. **大黑板的失敗教訓**：老師當年做的是實體看板（Kanban），最後因為無法處理**先進先出（FIFO）**、資料寫滿而卡死公司。本系統必須解決 FIFO 與歷史查詢。

### 個案討論題（系統必須回答）

1. 誰要做什麼決策？需要什麼資訊？要蒐集真實世界的哪些資料？
2. 如何透過 (a) 建立數位化模型的架構、(b) 確保模型與真實世界同步、(c) 提供決策支援 三步驟來建置系統？

## 2. 開發流程（How）—— 嚴格照此順序

```
環境搭建 → 產品設計 → 技術設計 → 產品實現 → 人工驗證
 (Git,      (產品方案    (技術方案)   (寫代碼      (人來測)
 AGENTS.md)  + Demo)                 + AI 自測)
```

| 階段 | 產出物 | 誰做 | 狀態 |
|------|--------|------|------|
| 1. 環境搭建 | Git repo、本檔 | 人 + AI | ✅ |
| 2. 產品設計 | `docs/01-產品方案.md`（含討論題 1 的答案）、`docs/demo/`（可點擊的畫面原型） | AI 起草，人確認 | ✅ |
| 3. 技術設計 | `docs/02-技術方案.md`（資料模型、API、技術選型、目錄結構） | AI 起草，人確認 | ✅ |
| 4. 產品實現 | 原始碼 + 自動化測試 | AI | ✅ |
| 5. 人工驗證 | `docs/03-驗證紀錄.md` | 人 | ✅ |

**規則**：
- 每個階段的文件經人確認（在文件頂端標記 `狀態：已確認`）之後，才能進入下一階段。
- 產品實現階段不得偷偷更改技術方案；要改先回去改文件。
- 每完成一個階段就 commit 一次，commit message 用中文，格式：`[階段] 做了什麼`，例如 `[產品設計] 完成產品方案與 demo`。

## 3. 領域概念（Ubiquitous Language）

| 中文 | 英文（程式碼用） | 說明 |
|------|------------------|------|
| 冷凍庫 | `Warehouse` | 兩座，編號 A、B |
| 櫃位 | `Slot` / `Location` | 標準化編號，如 `A-03-2`（庫-排-層） |
| 品項 | `Product` | 蔬果種類，約 20 種 |
| 批次 | `Batch` / `Lot` | 同一次入庫的同一品項，記錄入庫日期，是 FIFO 的單位 |
| 入庫 | `Inbound` | 貨到 → 指定櫃位 → 記錄 |
| 出庫 | `Outbound` | 客人要貨 → 系統建議最早批次與櫃位 → 搬 → 記錄 |
| 移位 | `Transfer` | 批次從一個櫃位搬到另一個櫃位 |
| 籠 | `Cage` | 計量單位（個案原文用「籠」） |

## 4. 技術約束

已由小組決定，技術設計階段只做細部設計，不再更換：

- **前端**：純 HTML + CSS + JavaScript（不用 React/Vue 等框架），由 Express 直接提供靜態檔。
- **後端**：Node.js + Express，用 `mysql2` 連線。
- **資料庫**：MySQL 8。
- **理由**：組員都不熟 Node.js，程式碼要**簡單、直白、有中文註解**，讓完全不會 Node 的人也能看懂並用 Claude 修改。不引入 TypeScript、ORM、打包工具。
- 繁體中文介面，手機/平板可操作（搬運工在冷凍庫門口用）。
- 資料不可遺失：所有異動寫入 MySQL 並保留歷史紀錄，不做實體刪除。
- 不接外部付費服務。啟動步驟必須寫在 `README.md`，目標是「裝 Node + MySQL、跑 schema.sql、`npm install`、`npm start`」四步以內。

## 5. 給 AI 代理的工作守則

- 使用**繁體中文**撰寫所有文件、UI 文字、commit message；程式碼識別字用英文。
- 動手前先確認目前處於哪個階段，只做該階段的事。
- 不確定需求時，回頭看 `竹南/` 的兩個 PDF 與本檔第 1 節，而不是自行腦補。
- 產品實現階段必須附測試，且自己跑過測試再交付。
- 不要在 repo 裡放大型二進位檔（PDF 除外，它是需求來源）。
