# 複製成 .env 再填（.env 不進 git）：cp .env.example .env
# MySQL 連線
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=你的MySQL密碼
DB_NAME=zhunan

# 網頁伺服器的 port
PORT=3000

# 時區：伺服器算「今天」、MySQL 記錄時間都用台灣時間
TZ=Asia/Taipei
