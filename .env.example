# 複製成 .env（.env 不進 git）：cp .env.example .env
# 小組統一：MySQL root 密碼設 1234，複製後不用改；密碼不同的人只改自己的 .env
# MySQL 連線
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=1234
DB_NAME=zhunan

# 網頁伺服器的 port
PORT=3000

# 時區：伺服器算「今天」、MySQL 記錄時間都用台灣時間
TZ=Asia/Taipei
